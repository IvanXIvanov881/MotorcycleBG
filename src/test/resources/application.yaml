spring:
  jpa:
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: never
  mvc:
    hiddenmethod:
      filter:
        enabled: true

forex:
  api:
    key: test
    url: "http://example.com"
    base: "USD"

admin:
  email: admin@example.com
  password: password
  firstName: Ivan
  lastName: Ivanov